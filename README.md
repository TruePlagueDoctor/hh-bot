# 📦 HH Job Finder Bot

Telegram-бот для поиска вакансий на HH.ru, генерации адаптированных резюме, сопроводительных писем и оценки комфортности вакансий через LLM.

---

## 🚀 Возможности

### 🧑‍💼 Регистрация пользователя (`/start`)

Бот создаёт профиль пользователя и сохраняет:

* Telegram ID
* ФИО
* город
* желаемую должность
* навыки
* базовое резюме (вводится отдельно через `/resume`)

---

### 🎛 Настройки поиска вакансий (`/search_settings`)

Пошаговый мастер с кнопками. Пользователь указывает:

* желаемую должность
* город
* минимальная зарплата
* станции метро
* свежесть вакансий (1–3 дня)
* тип занятости
* уровень опыта
* только прямые работодатели
* размер компании
* только ТОП-компании

Все фильтры сохраняются.

---

### 🔎 Поиск и просмотр вакансий (`/vacancies`)

Бот:

1. Загружает новые вакансии через API HH.ru по индивидуальным фильтрам.
2. Исключает уже отправленные / скрытые вакансии.
3. Показывает 5–10 подходящих вариантов.

Под каждой вакансией — кнопки:

* 📄 Сгенерировать резюме
* ✉️ Сопроводительное письмо
* 💚 Оценка по комфорту
* ❌ Неинтересно

---

### 🤖 Генерация документов (LLM)

Используется OpenAI-совместимый API (OpenRouter, LM Studio, vLLM, FastAPI-обёртка и т.д.).

Поддерживается:

* адаптированное резюме по вакансии
* сопроводительное письмо
* **оценка вакансии по комфортности** (1–10, плюсы/минусы/риски)
* экспорт резюме и письма в **PDF** с поддержкой кириллицы (шрифт DejaVuSans)

Настройки модели — в `.env`:

```
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=
```

---

### 📝 Базовое резюме (`/resume`)

Позволяет ввести базовый текст резюме.
Используется как исходная информация при адаптации под вакансию.

---

### 📚 История вакансий (`/history`)

Показывает последние взаимодействия:

* название вакансии
* компания
* город
* статус (новая / отправлена / скрыта)
* есть ли сгенерированные документы
* ссылки

---

### 📨 Ежедневная рассылка

Фоновый планировщик APScheduler:

* берет фильтры всех пользователей
* получает новые вакансии
* сохраняет их
* отправляет подборку пользователю
* отмечает отправленные вакансии, чтобы не присылать повторно

---

## ⚙️ Технологии

* Python 3.11+
* Aiogram 3
* PostgreSQL (Neon)
* SQLAlchemy 2 + asyncpg
* APScheduler
* ReportLab (PDF)
* HTTPX
* OpenAI-совместимый LLM endpoint

---

## 🗂 Структура проекта

```
hh-bot/
│
├── app/
│   ├── handlers/        # Telegram handlers
│   │   ├── start.py
│   │   ├── search_settings.py
│   │   ├── vacancies.py
│   │   ├── resume.py
│   │   └── history.py
│   │
│   ├── services/        # hh_service, llm_service, pdf utils, scheduler
│   │   ├── hh_service.py
│   │   ├── llm_service.py
│   │   ├── scheduler.py
│   │   └── pdf_utils.py
│   │
│   ├── db/              # async SQLAlchemy
│   │   ├── models.py
│   │   ├── crud.py
│   │   └── session.py
│   │
│   ├── utils/
│   │   └── keyboards.py
│   │
│   ├── fonts/           # DejaVuSans.ttf (для PDF)
│   │
│   └── config.py        # загрузка env
│
├── main.py              # запуск бота
├── requirements.txt
└── .env.example
```

---

## 🔧 Установка и запуск

### 1. Клонируем проект:

```bash
git clone https://github.com/TruePlagueDoctor/hh-bot
cd hh-bot
```

### 2. Создаём виртуальное окружение:

```bash
python -m venv .venv
source .venv/bin/activate      # Linux/Mac
.venv\Scripts\activate         # Windows
```

### 3. Устанавливаем зависимости:

```bash
pip install -r requirements.txt
```

### 4. Настраиваем `.env`

Создать файл `.env` по образцу `.env.example`:

```
TG_BOT_API_KEY=
DATABASE_URL=
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=
```

### 5. Запускаем:

```bash
python -m app.main
```

---

## 🧰 Требования к базе данных

Neon Postgres (или любой PostgreSQL).

База создаётся автоматически SQLAlchemy ORM при первом запуске.

Если нужны Alembic-миграции — можно подключить позже (структура к этому готова).

---

## 📄 Генерация PDF

PDF создаются:

* с кириллицей (шрифт DejaVuSans)
* со страницами, переносами строк
* с заголовком
* чистым форматированием

---

## 🛡 Нефункциональные требования

Бот учитывает:

* логирование без утечки секретов
* устойчивость к ошибкам HH.ru / LLM
* отсутствие дублей вакансий
* корректную многопользовательскую работу
* масштабируемость архитектуры

---
