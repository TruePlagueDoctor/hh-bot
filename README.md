🧑‍💼 Telegram-бот для поиска вакансий на hh.ru

Этот проект — асинхронный Telegram-бот, который ищет вакансии на hh.ru по индивидуальным фильтрам пользователя, ежедневно присылает новые вакансии и позволяет генерировать адаптированное резюме и сопроводительное письмо через LLM.

🚀 Основные возможности
🔹 Регистрация пользователя — /start

Пользователь создаёт профиль и отправляет сообщение в формате:

ФИО
Город
Желаемая должность
Навыки через запятую


Сохраняются:

Telegram ID

ФИО

Город

Желаемая должность

Навыки

Базовое резюме (через команду /resume)

🔹 Настройка фильтров поиска — /search_settings

Бот запускает диалоговый мастер и спрашивает:

желаемая должность;

город;

минимальная зарплата;

станции метро (опционально);

«свежесть» вакансий (1–3 дня);

тип занятости: полная / частичная / удалённая;

опыт работы: нет / 1–3 / 3–6 / 6+ лет;

только прямые работодатели (да/нет);

размер компании: малая / средняя / крупная / пропустить;

только ТОП-компании (да/нет).

Все данные сохраняются в таблицу search_filters.

🔹 Базовое резюме — /resume

Пользователь присылает текст своего базового резюме.
Бот сохраняет его в User.base_resume.

При генерации адаптированного резюме оно используется в промпте LLM.

🔹 Просмотр вакансий — /vacancies

Бот:

Обновляет вакансии через hh.ru по текущим фильтрам пользователя;

Сохраняет новые вакансии в PostgreSQL;

Показывает 5 свежих вакансий.

Каждая вакансия содержит:

название

компания

город

зарплата

ссылка на hh.ru

Под вакансией — кнопки:

📄 Сгенерировать резюме

✉️ Сопроводительное письмо

❌ Неинтересно (вакансия больше не показывается)

🔹 Генерация документов

Бот использует OpenAI-совместимый API (например, OpenRouter).

Сохраняются:

адаптированное резюме;

сопроводительное письмо;

В таблицу generated_documents.

🔹 Ежедневная рассылка (APSheduler)

Каждый день бот:

берёт фильтры всех пользователей;

делает запрос на hh.ru;

сохраняет новые вакансии;

присылает подборку (5–10 вакансий);

помечает вакансии как отправленные (без повторов).

🛠 Технологии

Python 3.11+

aiogram 3.x

PostgreSQL (Neon)

SQLAlchemy 2.0 (async)

asyncpg

APScheduler

httpx

OpenAI-совместимый LLM endpoint

📦 Структура проекта
project/
  app/
    handlers/
      start.py
      search_settings.py
      resume.py
      vacancies.py
    services/
      hh_service.py
      llm_service.py
      scheduler.py
    db/
      models.py
      session.py
      crud.py
    config.py
  README.md
  .env

⚙️ Установка и запуск
1. Клонировать репозиторий
git clone ...
cd project

2. Создать виртуальное окружение
python -m venv .venv
.\.venv\Scripts\activate      # Windows

3. Установить зависимости
pip install -r requirements.txt

4. Создать файл .env

Пример:

TG_BOT_API_KEY=your_bot_token
DATABASE_URL=postgresql+asyncpg://user:pass@host/dbname
LLM_BASE_URL=https://openrouter.ai/api
LLM_API_KEY=your_llm_key
LLM_MODEL_NAME=meta-llama/llama-3.1-8b-instruct:free

5. Создать таблицы

Если используешь Alembic:

alembic upgrade head


Иначе:

python -m app.init_db_schema

6. Запустить бота
python -m app.main

🧪 Тестирование

Команды:

/start — создать профиль

/resume — загрузить базовое резюме

/search_settings — настроить фильтры

/vacancies — проверить поиск

кнопка «Сгенерировать резюме» — тест LLM

кнопка «Неинтересно» — тест логики фильтрации

🔐 Безопасность

Секреты не логируются.

API ключи не сохраняются в чат.

Бот не повторяет рассылку тех же вакансий.

Ошибки LLM и hh.ru отрабатываются корректно.

📄 Лицензия

MIT.